/**
 * This is the environment configuration for development environments.
 * Adjust angular.json to replace this file with the correct one
 * depending on the actual environment.
 */
export const environment = {
  production: false,
  ONLY_ARTISTS: false,
}
